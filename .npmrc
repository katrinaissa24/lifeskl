# Use a flat (hoisted) node_modules layout.
# This keeps pnpm friendly with React Native / Metro when we add apps/mobile,
# which historically dislikes pnpm's default symlinked layout.
node-linker=hoisted
